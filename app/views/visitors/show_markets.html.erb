
<h2>Here is a list of all markets:</h2>
<% @markets.each do |market| %>
<article>
  <p>
    <%= link_to "#{market.name}", show_all_path(market.id) %>
    <%= market.state %>
  </p>
  <% end %>
</article>

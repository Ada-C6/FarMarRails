<% @product_list.each do |product| %>
  <p><%= link_to(product[:name], vendor_product_path(product[:vendor_id], product[:id]))%> <%= link_to("Edit ", edit_vendor_product_path(product[:vendor_id], product[:id]))%><%= link_to("Delete", vendor_product_path(product[:vendor_id], product[:id]), method: :delete, data: { confirm: "Are you sure you want to delete this product?"} ) %></p>
<% end %>
<br>
<p><%= link_to("New Product", new_vendor_product_path) %></p>

<% @product_list.each do |product| %>
  <p><%= link_to(product[:name], vendor_product_path(product[:vendor_id], product[:id]))%> <%= link_to("Edit", edit_vendor_product_path(product[:vendor_id], product[:id]))%></p>
<% end %>
<br>
<p><%= link_to("New Product", new_vendor_product_path) %></p>

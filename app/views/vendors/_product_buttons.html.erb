<%= button_to "Edit Product", edit_product_path(product_id), method: :get %>

<%= button_to("Delete Product", delete_product_path(product_id), method: :delete, data: {confirm: "Are you sure? You can't undo this."}) %>
